---
title: Frontmatter Fixture
tags: [fixture, frontmatter]
date: 2026-01-01
---

# Frontmatter Fixture

This document opens with a YAML frontmatter block. It exists to
verify that frontmatter is parsed when present and does not break
rendering when it is absent elsewhere in the tree (see
`with-frontmatter.md`'s siblings, which have none).

## Body

Ordinary content follows the frontmatter block.
