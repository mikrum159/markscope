# Should Not Show (Fixture)

This file lives under a folder name intended to be matched by a
built-in ignore pattern once the explorer/ignore-service unit
defines that pattern list. Until then, it is inert fixture content.
