# Architecture (Fixture)

A plain sibling document with no special formatting, used to check
that ordinary Markdown files show up in the tree and preview without
any GFM extras getting in the way.

## Overview

This is placeholder prose for smoke testing. It exists only so the
file has more than a heading in it.

## Components

- Component A
- Component B

## Notes

Nothing here is real system design — it is fixture content only.
